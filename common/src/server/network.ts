import { CommonGlobalEvents, CommonGlobalFunctions } from "../shared/network";

export const CommonEvents = CommonGlobalEvents.createServer({});
export const CommonFunctions = CommonGlobalFunctions.createServer({});