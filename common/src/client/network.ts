import { CommonGlobalEvents, CommonGlobalFunctions } from "../shared/network";

export const CommonEvents = CommonGlobalEvents.createClient({});
export const CommonFunctions = CommonGlobalFunctions.createClient({});